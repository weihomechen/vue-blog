/* eslint-disable no-param-reassign */

const cate = {
  state: {
    list: [],
  },

  mutations: {
    LIST_CHANGE: (state, list) => {
      state.list = list;
    }
  },

  actions: {

  }
};

export default cate;
